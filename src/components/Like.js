import React from 'react';
// import { connect } from 'react-redux';

const Like = ({ likes }) => {
  return <a>Likes: {likes}</a>;
};

// function mapStateToProps({ }) {
//     {likes: }
// }

export default Like;
